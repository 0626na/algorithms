{
  "compilerOptions": {
    "target": "ES6", // ES6 이상의 버전
    "module": "commonjs", // CommonJS 모듈 사용
    "lib": ["es6"], // 필요한 라이브러리 포함
    "typeRoots": ["./node_modules/@types"], // 타입 정의 파일의 경로
    "types": ["node"], // Node.js 타입 정의 포함
    "esModuleInterop": true, // ES 모듈 호환성 설정
    "strict": true, // 엄격한 타입 체크
    "skipLibCheck": true // 라이브러리 타입 검사 생략 (선택 사항)
  },
  "include": ["**/*.ts"], // 컴파일할 파일 포함
  "exclude": ["node_modules"] // 컴파일에서 제외할 파일
}
